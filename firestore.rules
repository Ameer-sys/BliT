rules_version = '2';

// These rules are for prototype/demo only. Do not use with real patient data.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
